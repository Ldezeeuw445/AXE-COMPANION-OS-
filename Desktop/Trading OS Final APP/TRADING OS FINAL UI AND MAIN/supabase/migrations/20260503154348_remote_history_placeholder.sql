-- Placeholder: version 20260503154348 exists on remote `supabase_migrations.schema_migrations`
-- but had no matching file in this repo. Aligns local CLI history with the remote project.
-- Safe no-op when applied on a fresh DB; on linked remote this version is already recorded and will be skipped.
SELECT 1;
